# Copy to `.env` and adjust credentials. Prisma requires DATABASE_URL to be set.
# Create the database first: CREATE DATABASE peoplesolve;
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/peoplesolve"

PORT=4000
CORS_ORIGIN=http://localhost:3000
